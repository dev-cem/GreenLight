/// Single source of truth for the game/studio names shown across the app —
/// the home screen, splash screen, and the Influencer Mode video overlay all
/// read from here instead of duplicating literals.
const String kGameName = 'Greenlight';
const String kStudioName = 'Kroma Games';

/// This game's letter in the RGB suite mark (see widgets/suite_badge.dart) —
/// Retrace is R (red), Greenlight is G (green), a future game completes the
/// set as B (blue).
const String kSuiteLetter = 'G';
